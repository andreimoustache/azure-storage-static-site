trigger:
- master

pool:
  vmImage: 'win1803'

variables:
- group: 'AzureConnections'

steps:
- task: AzureFileCopy@3
  inputs:
    azureSubscription: $(Subscription)
    destination: 'AzureBlob'
    storage: $(StorageName)
    containerName: '$web'
